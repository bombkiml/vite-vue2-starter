import Auth from "@/views/Authentication/Auth.vue";
import Template from "@/views/Template/Frontend.vue";

let components = {
  Auth,
  Template,
};

export default components;

export { Auth, Template };
